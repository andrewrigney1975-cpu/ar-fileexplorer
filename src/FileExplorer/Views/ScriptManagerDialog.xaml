<UserControl
    x:Class="FileExplorer.Views.ScriptManagerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Width="1100" HorizontalAlignment="Left" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="560" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: saved scripts -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="+ New Script" HorizontalAlignment="Stretch" Margin="0,0,0,8" Click="NewScript_Click" />

            <ListView Grid.Row="1" x:Name="ScriptsList" SelectionMode="Single" SelectionChanged="ScriptsList_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnSpacing="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            <Button Grid.Column="1" Style="{StaticResource ToolbarIconButtonStyle}" Padding="4"
                                    Click="RenameScript_Click" Tag="{Binding}" ToolTipService.ToolTip="Rename script">
                                <FontIcon Glyph="&#xE8AC;" FontSize="12" />
                            </Button>
                            <Button Grid.Column="2" Style="{StaticResource ToolbarIconButtonStyle}" Padding="4"
                                    Click="DeleteScript_Click" Tag="{Binding}" ToolTipService.ToolTip="Delete script">
                                <FontIcon Glyph="&#xE74D;" FontSize="12" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Right: editor -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="130" />
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0" x:Name="NameBox" PlaceholderText="Script name" Margin="0,0,0,8" />

            <TextBox Grid.Row="1" x:Name="CodeBox" AcceptsReturn="True" TextWrapping="NoWrap"
                     FontFamily="Cascadia Mono, Consolas" FontSize="12"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" />

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,8,0,8" VerticalAlignment="Center">
                <Button Content="Save" Click="Save_Click" />
                <Button Content="Run" Click="Run_Click" />
                <ProgressRing x:Name="RunningRing" IsActive="False" Width="18" Height="18" />
                <Button Content="API Reference...">
                    <Button.Flyout>
                        <Flyout Placement="Bottom">
                            <Flyout.FlyoutPresenterStyle>
                                <Style TargetType="FlyoutPresenter">
                                    <Setter Property="MaxWidth" Value="1000" />
                                    <Setter Property="MaxHeight" Value="560" />
                                </Style>
                            </Flyout.FlyoutPresenterStyle>
                            <ScrollViewer MaxWidth="980" MaxHeight="520" HorizontalScrollBarVisibility="Auto">
                                <TextBlock x:Name="ReferenceText" FontFamily="Cascadia Mono, Consolas" FontSize="12" TextWrapping="NoWrap" />
                            </ScrollViewer>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>

            <TextBox Grid.Row="3" x:Name="OutputBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                     FontFamily="Cascadia Mono, Consolas" FontSize="11" Opacity="0.85"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" />
        </Grid>
        </Grid>

        <Button Grid.Row="1" x:Name="CloseButtonElement" Content="Close" Width="120" HorizontalAlignment="Right" Click="Close_Click" />
    </Grid>
</UserControl>
