<UserControl
    x:Class="FileExplorer.Views.SearchEverywhereDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Width="820" Height="600" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" x:Name="QueryBox" PlaceholderText="Search every indexed file and folder by name..."
                  FontSize="18" TextChanged="QueryBox_TextChanged" KeyDown="QueryBox_KeyDown"
                  AutomationProperties.Name="Search everywhere" />

        <TextBlock Grid.Row="1" x:Name="StatusText" Opacity="0.65" FontSize="12" TextWrapping="Wrap" />

        <Grid Grid.Row="2">
            <ListView x:Name="ResultsList" SelectionMode="Single"
                      DoubleTapped="ResultsList_DoubleTapped" KeyDown="ResultsList_KeyDown"
                      AutomationProperties.Name="Search results">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnSpacing="10" Padding="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0" Glyph="{Binding Glyph}" FontFamily="Segoe Fluent Icons" FontSize="16" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding DirectoryPath}" Opacity="0.6" FontSize="12" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="{Binding SizeDisplay}" Opacity="0.7" FontSize="12" VerticalAlignment="Center" Width="70" TextAlignment="Right" />
                            <TextBlock Grid.Column="3" Text="{Binding ModifiedDisplay}" Opacity="0.7" FontSize="12" VerticalAlignment="Center" Width="120" TextAlignment="Right" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel x:Name="EmptyStatePanel" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8" MaxWidth="420">
                <TextBlock x:Name="EmptyStateText" Opacity="0.65" FontSize="13" TextWrapping="Wrap" TextAlignment="Center" />
                <HyperlinkButton x:Name="EmptyStateLink" Content="Manage Search Index..." Click="ManageIndex_Click" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <HyperlinkButton Grid.Column="0" Content="Manage Search Index..." Click="ManageIndex_Click" Padding="0" />
            <Button Grid.Column="2" Content="Close" Width="120" Click="Close_Click" />
        </Grid>
    </Grid>
</UserControl>
